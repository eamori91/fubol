{% extends 'base.html' %}

{% block title %}Resultados de Consulta{% endblock %}

{% block content %}
<div class="container mt-4">
    <h1 class="mb-4">Resultados de Consulta</h1>
    
    <div class="card mb-4">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0">Consulta ejecutada</h5>
        </div>
        <div class="card-body">
            <pre>{{ consulta }}</pre>
        </div>
    </div>

    <div class="card">
        <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Resultados</h5>
            <span class="badge bg-light text-dark">{{ datos|length }} filas</span>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                {% if datos %}
                <table class="table table-striped table-hover mb-0">
                    <thead class="table-dark">
                        <tr>
                            {% for columna in columnas %}
                            <th>{{ columna }}</th>
                            {% endfor %}
                        </tr>
                    </thead>
                    <tbody>
                        {% for fila in datos %}
                        <tr>
                            {% for valor in fila %}
                            <td>{{ valor }}</td>
                            {% endfor %}
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
                {% else %}
                <div class="alert alert-info m-3">
                    La consulta no devolvió resultados.
                </div>
                {% endif %}
            </div>
        </div>
        <div class="card-footer">
            <a href="{{ url_for('explorar_db') }}" class="btn btn-primary">Volver al explorador</a>
            <a href="{{ url_for('index') }}" class="btn btn-secondary">Volver al inicio</a>
        </div>
    </div>
</div>
{% endblock %}
